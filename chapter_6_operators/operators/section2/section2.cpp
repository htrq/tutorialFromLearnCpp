#include "section2.h"
#include <iostream>

/*
int integerResultZeroDivide()
{
    return 0/0; // on my platform not showing result at all just crashes
                // in debugger operating system returns signal SIGFPE floating point error
}
*/

void section2()
{
    //std::cout << integerResultZeroDivide() << '\n';   // commented out because crashes
    std::cout << "just a tip: better to use y += 4 than y = y + 4 it takes just one operand\n";
}
